namespace eBook.Mobile.Shared.Platforms.Android.CurrentActivity
{
    public enum ActivityEvent
    {
        Created,
        Resumed,
        Paused,
        Destroyed,
        SaveInstanceState,
        Started,
        Stopped
    }
}